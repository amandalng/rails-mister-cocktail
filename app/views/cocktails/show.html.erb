<h1><%= @cocktail.name %></h1>

<ul>
  <% @doses.each do |dose| %>
    <li><%= dose.description %> <%= dose.ingredient.name %> | <%= link_to "Delete", [@cocktail, dose], method: :delete, data: {confirm: "Are you sure?"} %>
  <% end %>
</ul>

<%= link_to "Add a dose", new_cocktail_dose_path(@cocktail)  %> |
<%= link_to "See all cocktails", cocktails_path %>
